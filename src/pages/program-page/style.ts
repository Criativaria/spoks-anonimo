import { CreateStyle } from "../../utils/create-style";

export const makeStyle = CreateStyle((theme) => ({

    list: {
        overflow: "hidden",
        height: 600,
        marginVertical: 10
    }
}));